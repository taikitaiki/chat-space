.side-bar
  .user
    .user__name
      = current_user.name
    %ul.user__elements
      %ls.new
      = link_to do
        = fa_icon "edit", class: 'icon'
      %ls.cog
      = link_to edit_user_path(current_user) do
        = fa_icon "cog", class: 'icon'

  .group-list
    .group
      .group__name
        sample group
      .group__message
        Hello World
    .group
      .group__name
        group name
      .group__message
        まだメッセージありません。
    .group
      .group__name
        group name
      .group__message
        まだメッセージありません。
